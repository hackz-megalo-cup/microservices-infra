apiVersion: v1
kind: Service
metadata:
  name: sample-app
  namespace: sample-app
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app.kubernetes.io/name: sample-app
    app.kubernetes.io/version: 0.1.0

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgresql-capture
  namespace: argocd
spec:
  destination:
    namespace: database
    server: https://kubernetes.default.svc
  project: default
  source:
    path: ./manifests/postgresql-capture
    repoURL: https://github.com/hackz-megalo-cup/microservices-infra
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
